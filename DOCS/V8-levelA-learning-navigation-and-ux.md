# V8 – Level A: Learning Navigation & UX

ב־V7 כבר קיימים:

getUnlockedLayerSnapshotA(child, 'A')

learn.layer.\* ב־i18n (title/desc/header)

ולכן ב־V8 לא צריך להמציא טקסטים לשכבות (כמו “שכבה 0”), אלא לצרוך i18n הקיים.

## סטטוס

📌 גרסה בתהליך (In Progress)  
📍 מגיעה אחרי V7 סגור ויציב

---

## רקע

אנחנו מגיעים מ־V7 שבו:

- כל הלוגיקה של Level A קיימת ועובדת:
  - שכבות (layers)
  - חבילות / קבוצות (groups / packs)
  - יחידות (units)
  - gating, progress, recommendations
- ה־policy יציב ונעול

ב־V7 נוספו Layer headers במסך הלמידה,  
אך בפועל המסך הפך לעמוס מדי עבור ילד:

- יותר מדי אפשרויות
- חוסר היררכיה ברורה
- עומס קוגניטיבי

הוחלט **לא** לשפר זאת ב־V7, אלא לפתוח גרסה ייעודית — V8 — שמתמקדת אך ורק ב־UX וניווט.

---

## החלטת מוצר (נעולה)

### מבנה ניווט חדש ללמידה

1. מסך הלמידה הראשי מציג **שכבות בלבד** (0–4)
2. לחיצה על שכבה מובילה למסך חדש
3. במסך שכבה מוצגות **רק החבילות / הקבוצות של אותה שכבה**

❗ אין dropdowns  
❗ אין ערבוב שכבות  
❗ אין קיצורי דרך

היררכיה קשיחה:

---

## מטרות V8

### 1. הפחתת עומס

- הילד רואה בכל רגע רמת עומק אחת בלבד
- אין הצפה של תוכן עתידי

### 2. מיקוד

- התקדמות שכבה־אחר־שכבה
- חיזוק תחושת “אני יודע איפה אני”

### 3. שימוש מלא בתשתית קיימת

- אין שינוי ב־policy
- אין שינוי ב־thresholds
- אין hardcode חדש

---

## מה לא עושים ב־V8

❌ לא נוגעים ב־Content  
❌ לא משנים מדיניות פתיחה / סגירה  
❌ לא מחזירים לוגיקה למסכים  
❌ לא פותחים Level B ומעלה

---

## Flow כללי

Parent / Child Home
↓
LearnHomeScreen
(רשימת שכבות)
↓
LearnLayerScreen
(חבילות של שכבה אחת)
↓
Group / Unit Flow

ניווט חזרה:

- תמיד רמה אחת למעלה
- אין קפיצות

---

## Wireframe טקסטואלי

### LearnHomeScreen — מסך שכבות

מה מוצג:

- רשימה אנכית של שכבות בלבד
- לכל שכבה:
  - שם
  - חיווי מצב: פתוחה / נוכחית / נעולה
  - התקדמות כללית לשכבה

מה לא מוצג:

- חבילות
- יחידות
- משחקים
- תוכן לימודי

אינטראקציה:

- לחיצה על שכבה פתוחה / נוכחית → מעבר למסך שכבה
- שכבה נעולה → ללא פעולה (או חיווי עתידי)

---

### LearnLayerScreen — מסך שכבה

Header:

- כפתור חזרה
- שם השכבה

מה מוצג:

- רשימת חבילות / קבוצות של השכבה בלבד
- לכל חבילה:
  - שם
  - מספר יחידות
  - חיווי מצב
  - התקדמות

מה לא מוצג:

- שכבות אחרות
- ניווט צדדי
- תפריטים גלובליים

אינטראקציה:

- לחיצה על חבילה פתוחה / נוכחית → כניסה לזרימת היחידות
- חזרה → LearnHomeScreen

---

## פירוק לאחריות קומפוננטות

### LearnHomeScreen

אחראי על:

- הצגת שכבות
- סטטוס שכבה
- התקדמות כללית
- ניווט למסך שכבה

לא אחראי על:

- חבילות
- יחידות
- unlocking logic

---

### LearnLayerScreen

אחראי על:

- הצגת חבילות של שכבה אחת
- סטטוס חבילה
- התקדמות פר חבילה
- ניווט קדימה / אחורה

לא אחראי על:

- שכבות אחרות
- policy
- state גלובלי

---

### UI Components (תצוגה בלבד)

#### LayerCard

- קלט: layerId, status, progress
- ללא לוגיקה

#### GroupCard

- קלט: groupId, status, progress
- ללא לוגיקה

---

## שימוש בתשתית קיימת (V7)

V8 **צורך** בלבד:

- `minLayer`
- `group.policy`
- `unit.policy`
- `getUnlockedLayerSnapshotA`
- recommendation קיימת (אם יש)

❗ אם אין פונקציה — לא ממציאים.

---

## Hooks לעתיד (לא חלק מ־V8)

- “מטרת שכבה” (טקסט קצר)
- מסר להורה במסך שכבה
- אנימציית פתיחת שכבה
- חיווי “סיימת שכבה 🎉”

---

## מה ננעל במסמך זה

- מבנה הניווט
- היררכיית המסכים
- חלוקת אחריות
- גבולות הגזרה של V8

מסמך זה משמש כ־**מקור אמת** ל־V8.
